---
# prefer lego because synapse uses a version of python3-zope.interface that conflicts with certbot
acme_email: acme@bitcoiner.social
acme_domains:
  - { domain: "{{ inventory_hostname }}", provider: easydns }
  - { domain: bitcoiner.social, provider: easydns }
  - { domain: mail.bitcoiner.social, provider: easydns }
  - { domain: nostr.bitcoiner.social, provider: easydns }
